"""Executes program with user interaction."""

from datetime import datetime

from src.load import extract_subcomms
from utilities import SUBR


def main() -> None:
    """Execute sample programs."""
    """
    start_date = datetime(2019, 12, 10)
    posts = extract_subcomms(SUBR, start_date, limit=10)
    breakpoint()
    """


if __name__ == "__main__":
    main()
